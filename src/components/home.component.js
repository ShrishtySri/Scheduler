import React, { useContext } from "react";
export default function Home() {
 

  return (
    <div className="displayone">
      <img src={ require('./display.jpg') } height={660} width={1100} />
    </div>
  );
}